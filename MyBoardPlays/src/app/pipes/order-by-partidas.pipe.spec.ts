import { OrderByPartidasPipe } from './order-by-partidas.pipe';

describe('OrderByPartidasPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByPartidasPipe();
    expect(pipe).toBeTruthy();
  });
});
