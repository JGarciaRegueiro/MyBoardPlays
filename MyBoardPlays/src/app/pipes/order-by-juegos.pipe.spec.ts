import { OrderByJuegosPipe } from './order-by-juegos.pipe';

describe('OrderByJuegosPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByJuegosPipe();
    expect(pipe).toBeTruthy();
  });
});
